import { Given, When, Then } from "cucumber";

//TelaMapa.feature
Given("um caminho de um hospital mais proximo", (hospital) => {});

When("dados dos exames estao abaixo do esperado", () => {});

Then("será direcionado para o hospital mais proximo", () => {});
